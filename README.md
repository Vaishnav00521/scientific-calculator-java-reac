# 🧮 Scientific Calculator (Full Stack)

A colorful, mobile-style scientific calculator built with **Java Spring Boot** and **React.js**.

## ✨ Features
- **Scientific Operations:** Sin, Cos, Log, and Sqrt powered by a Java Backend.
- **Modern UI:** Light theme with a 4-column mobile grid layout.
- **Animations:** Smooth transitions using Framer Motion.
- **History:** Slide-out sidebar to track previous calculations.

## 🚀 How to Run
### 1. Backend (Java)
- Open `scientific-backend` in IntelliJ.
- Run `ScientificBackendApplication.java`.
- Server starts on `http://localhost:8080`.

### 2. Frontend (React)
- Navigate to `scientific-ui`.
- Run `npm install` and then `npm start`.
- App starts on `http://localhost:3000`.
